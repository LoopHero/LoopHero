package unsw.loopmania.allies;
/**
 * A interface for different defending Strategy pattern, 
 * which describe how damage taken would be mitigated.
 */
public interface DefenseStrategy{
    public int defence (int dmg);
}
